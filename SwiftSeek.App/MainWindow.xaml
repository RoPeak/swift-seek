<Window
    x:Class="SwiftSeek.App.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SwiftSeek.App"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="SwiftSeek">

    <Grid Padding="16" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Spacing="12">
            <TextBox x:Name="SearchTermBox" Width="420" PlaceholderText="Search term" />
            <Button x:Name="SearchButton" Content="Search" Click="OnSearchClicked" />
        </StackPanel>

        <Grid Grid.Row="1" ColumnSpacing="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="320" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <ScrollViewer Grid.Column="0">
                <StackPanel Spacing="10">
                    <TextBlock Text="Filters" FontSize="18" FontWeight="SemiBold" />

                    <TextBlock Text="Root directory" />
                    <TextBox x:Name="RootDirectoryBox" PlaceholderText="e.g. C:\Projects" Text="." />

                    <CheckBox x:Name="ContentSearchBox" Content="Search file contents" />
                    <CheckBox x:Name="RegexBox" Content="Use regular expressions" />
                    <CheckBox x:Name="CaseSensitiveBox" Content="Case-sensitive" />
                    <CheckBox x:Name="PhraseBox" Content="Exact phrase" />
                    <CheckBox x:Name="FuzzyBox" Content="Fuzzy matching (index only)" />

                    <TextBlock Text="Content search mode" />
                    <ComboBox x:Name="ContentModeBox" SelectedIndex="0">
                        <ComboBoxItem Content="Auto" />
                        <ComboBoxItem Content="Index" />
                        <ComboBoxItem Content="Scan" />
                    </ComboBox>

                    <TextBlock Text="Include extensions (comma-separated)" />
                    <TextBox x:Name="IncludeExtensionsBox" PlaceholderText=".cs,.md" />

                    <TextBlock Text="Exclude extensions (comma-separated)" />
                    <TextBox x:Name="ExcludeExtensionsBox" PlaceholderText=".dll,.exe" />

                    <TextBlock Text="Minimum size (bytes)" />
                    <TextBox x:Name="MinSizeBox" PlaceholderText="0" />

                    <TextBlock Text="Maximum size (bytes)" />
                    <TextBox x:Name="MaxSizeBox" PlaceholderText="26214400" />
                </StackPanel>
            </ScrollViewer>

            <Grid Grid.Column="1" ColumnSpacing="16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="3*" />
                    <ColumnDefinition Width="2*" />
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Background="#1F000000" CornerRadius="8" Padding="8">
                    <StackPanel>
                        <TextBlock Text="Results" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,8" />
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="2*" />
                                <ColumnDefinition Width="3*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="Path" FontWeight="SemiBold" />
                            <TextBlock Text="Snippet" FontWeight="SemiBold" Grid.Column="1" />
                        </Grid>
                        <ListView x:Name="ResultsList" SelectionChanged="OnResultSelected" Margin="0,8,0,0">
                            <ListView.ItemTemplate>
                                <DataTemplate x:DataType="local:SearchResultItem">
                                    <Grid Padding="4">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="2*" />
                                            <ColumnDefinition Width="3*" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="{x:Bind Path}" TextTrimming="CharacterEllipsis" />
                                        <TextBlock Text="{x:Bind Snippet}" Grid.Column="1" TextTrimming="CharacterEllipsis" />
                                    </Grid>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </StackPanel>
                </Border>

                <Border Grid.Column="1" Background="#1F000000" CornerRadius="8" Padding="8">
                    <StackPanel>
                        <TextBlock Text="Preview" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,8" />
                        <TextBlock x:Name="PreviewPath" Text="Select a result to preview" TextWrapping="Wrap" Margin="0,0,0,8" />
                        <ScrollViewer>
                            <TextBox x:Name="PreviewContent" IsReadOnly="True" TextWrapping="Wrap" AcceptsReturn="True" />
                        </ScrollViewer>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>

        <Grid Grid.Row="2" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock x:Name="StatusText" Text="Ready" />
                <ProgressBar x:Name="ProgressBar" Height="6" IsIndeterminate="False" />
            </StackPanel>
            <Button Grid.Column="1" x:Name="CancelButton" Content="Cancel" Click="OnCancelClicked" IsEnabled="False" />
        </Grid>
    </Grid>
</Window>
