<Window
    x:Class="SwiftSeek.App.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="SwiftSeek"
    Width="800"
    Height="600">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Top Bar -->
        <StackPanel Orientation="Horizontal" Background="LightGray" Padding="10">
            <TextBox Width="400" Margin="5" PlaceholderText="Search..." />
            <Button Content="Search" Width="100" Margin="5" />
        </StackPanel>

        <!-- Main Content -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Left Panel -->
            <StackPanel Background="LightBlue" Padding="10">
                <TextBlock Text="Scopes" FontWeight="Bold" />
                <!-- Add scope items here -->
            </StackPanel>

            <!-- Results Panel -->
            <ListView Grid.Column="1" Margin="10">
                <!-- Bind search results here -->
            </ListView>
        </Grid>
    </Grid>
</Window>